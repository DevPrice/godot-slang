<?xml version="1.0" encoding="UTF-8" ?>
<class name="SlangShaderProgram" inherits="Resource" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
	</brief_description>
	<description>
		A single compiled program within a [SlangShaderFile]. Contains both the compiled SPIR-V and reflection metadata related to this particular program within a shader.
		A compute program is built from a single entry-point; a raster program is built from a vertex and a fragment entry-point linked together. Both are listed in [member SlangShaderFile.programs] and told apart by [member stages].
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="has_stage" qualifiers="const">
			<return type="bool" />
			<param index="0" name="stage" type="int" enum="RenderingDevice.ShaderStage" />
			<description>
				Returns [code]true[/code] if this program is built from the given shader stage.
			</description>
		</method>
		<method name="get_compile_error" qualifiers="const">
			<return type="String" />
			<description>
				Returns the compile error reported for any stage of this program, or an empty string if it compiled successfully.
			</description>
		</method>
	</methods>
	<members>
		<member name="program_name" type="StringName" setter="set_program_name" getter="get_program_name" default="&amp;&quot;&quot;">
			The name of this kernel. By default, this will match the entry-point function name declared in the shader.
		</member>
		<member name="parameters" type="StructTypeLayoutShape" setter="set_parameters" getter="get_parameters">
			Get the reflection information of the parameters for this kernel. You typically won't need to access this, but it is used under the hood by [SlangShaderTask] to bind data to the correct descriptor slots.
		</member>
		<member name="slot_offset" type="int" setter="set_slot_offset" getter="get_slot_offset" default="0">
			The slot offset of this kernel's parameters, relative to the global parameters.
		</member>
		<member name="space_offset" type="int" setter="set_space_offset" getter="get_space_offset" default="0">
			The space offset of this kernel's parameters, relative to the global parameters.
		</member>
		<member name="spirv" type="RDShaderSPIRV" setter="set_spirv" getter="get_spirv">
			The SPIR-V intermediate representation of this program, with one bytecode entry per shader stage it provides.
		</member>
		<member name="stages" type="int" setter="set_stages" getter="get_stages" default="0">
			The shader stages this program is built from, as a bitmask of [code]RenderingDevice.SHADER_STAGE_*_BIT[/code] values. This is set even when a stage fails to compile, so it describes what the program is rather than what succeeded.
		</member>
		<member name="thread_group_size" type="Vector3i" setter="set_thread_group_size" getter="get_thread_group_size" default="Vector3i(0, 0, 0)">
			The thread group size declared for this kernel.
		</member>
		<member name="used_binding_sets" type="Dictionary" setter="set_used_binding_sets" getter="get_used_binding_sets" default="{}">
			The binding sets that are used by this kernel. The key is the binding set index, and the value is true/false.
		</member>
		<member name="user_attributes" type="Dictionary" setter="set_user_attributes" getter="get_user_attributes" default="{}">
			Reflection information about any declared attributes on the entry-point function.
		</member>
	</members>
</class>
